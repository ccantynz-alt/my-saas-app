/**
 * TEMP STUB:
 * Original file depended on '@/app/lib/kv' which doesn't exist (alias issue).
 * Keep exports so imports won't break.
 */
export async function trackEvent(_event: any) {
  return { ok: true, status: "stub" };
}

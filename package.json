{
  "name": "my-saas-app-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd app && npm install && npm run dev",
    "build": "cd app && npm install && npm run build",
    "start": "cd app && npm install && npm run start",
    "lint": "cd app && npm install && npm run lint",
    "vercel-build": "cd app && npm install && npm run build"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "zod": "^3.23.8"
  }
}

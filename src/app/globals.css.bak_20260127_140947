@tailwind base;
@tailwind components;
@tailwind utilities;

/* -------------------------------------------------------
   Dominat8 Flagship Polish Layer (WOW v1)
   Safe: pure CSS + Tailwind directives only
------------------------------------------------------- */

:root {
  color-scheme: dark;
}

html, body {
  height: 100%;
}

body {
  background: #05060A;
  color: rgba(255,255,255,0.92);
}

/* Better default text rendering */
* {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

/* Premium subtle grid */
.d8-grid {
  background-image:
    linear-gradient(to right, rgba(255,255,255,0.06) 1px, transparent 1px),
    linear-gradient(to bottom, rgba(255,255,255,0.06) 1px, transparent 1px);
  background-size: 56px 56px;
  mask-image: radial-gradient(60% 55% at 50% 35%, #000 55%, transparent 78%);
}

/* Soft noise overlay (pure CSS) */
.d8-noise {
  background-image:
    radial-gradient(circle at 20% 10%, rgba(255,255,255,0.06), transparent 40%),
    radial-gradient(circle at 80% 0%, rgba(255,255,255,0.05), transparent 40%),
    radial-gradient(circle at 50% 70%, rgba(255,255,255,0.04), transparent 45%);
  filter: blur(0.2px);
  opacity: 0.6;
}

/* Animated glow */
@keyframes d8Float {
  0%   { transform: translate3d(0, 0, 0) scale(1); opacity: 0.75; }
  50%  { transform: translate3d(0, -16px, 0) scale(1.03); opacity: 0.9; }
  100% { transform: translate3d(0, 0, 0) scale(1); opacity: 0.75; }
}

@keyframes d8Sheen {
  0%   { transform: translateX(-60%) rotate(12deg); opacity: 0; }
  15%  { opacity: 0.55; }
  50%  { opacity: 0.25; }
  100% { transform: translateX(160%) rotate(12deg); opacity: 0; }
}

/* Glass */
.d8-glass {
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.10);
  box-shadow:
    0 24px 70px rgba(0,0,0,0.55),
    inset 0 1px 0 rgba(255,255,255,0.08);
  backdrop-filter: blur(14px);
}

/* Button sheen */
.d8-sheen {
  position: relative;
  overflow: hidden;
}
.d8-sheen::after {
  content: "";
  position: absolute;
  inset: -40% -50%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.35), transparent);
  transform: translateX(-60%) rotate(12deg);
  opacity: 0;
  animation: d8Sheen 3.8s ease-in-out infinite;
}

/* Subtle separators */
.d8-hairline {
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.10), transparent);
}

/* Tiny badge glow */
.d8-badge {
  box-shadow: 0 18px 55px rgba(56,189,248,0.12);
}
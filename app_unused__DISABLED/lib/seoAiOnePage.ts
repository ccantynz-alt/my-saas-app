/**
 * TEMP STUB:
 * Original depended on '@/app/lib/openaiResponses' alias.
 * Keep export so routes that import this won't fail.
 */
export async function generateSeoOnePage(_input: any) {
  return {
    ok: false,
    status: "stub",
    message: "seoAiOnePage not implemented",
  };
}
